# NMFVis
Widget doesn't render, but looking at plotly fig as html for tuning. 
Widget doesn't render, but seems like a commong problem. havent tested on <atlas>. 
To do: 
 - widget ?
 - Sorting by dicts, next.
 - 
